<div class="form3">
<% if params[:user_id]%>
<% form_info = [@user, @event] %>
<% else %>
<% form_info = (@event) %>
<% end %>
 <%= form_for form_info do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="field_users_form" class="f_name">
    <p class="hack"><%= f.label :name %>:
    <%= f.text_field :name, placeholder: 'Name' %></p>
  </div>
  <div id="field_users_form" class="s_date">
     <p><%= f.label :start_date %>:
    <%= f.date_field :start_date, placeholder: 'Start Date' %></p>
  </div>
  <div id="field_users_form" class="e_date">
    <p><%= f.label :end_date %>:
    <%= f.date_field :end_date, placeholder: 'End Date' %></p>
  </div>
  <div class="field2 location">
    <p><%= f.label :location %>:
    <%= f.text_field :location, placeholder: 'Location' %></p>
  </div>
  <div class="field2 img">
    <p><%= f.label :image %>:
    <%= f.url_field :image, placeholder: 'Image' %></p>
  </div>
  <br />
   <div id="submit_center">
     <br/><br/>
    <p id="sub"></br></br><%= f.submit %></p>
  </div>
</br>
  <%= link_to 'back', user_path(id: @user.id) %>
<% end %>
</div>
